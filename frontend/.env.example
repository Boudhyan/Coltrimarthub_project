# Dev: use /api so Vite proxies to FastAPI (see vite.config.js). Works on any host/port.
VITE_API_URL=/api

# Production build: set your public API base URL (no trailing slash).
# VITE_API_URL=https://your-api.example.com
